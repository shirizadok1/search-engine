import React from 'react';
import { Client } from '@formspree/core';
interface Context {
    client: Client;
}
export interface Props {
    project?: string;
    children: React.ReactNode;
    stripePK?: string;
}
export declare const FormspreeProvider: (props: Props) => JSX.Element;
export declare function useFormspree(): Context;
export {};
