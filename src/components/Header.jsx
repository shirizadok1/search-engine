import React from "react";
import NavbarComponent from "./NavbarComponent";

function Header() {
    return (
        <header>
            <NavbarComponent />
        </header>
    );
}

export default Header;
